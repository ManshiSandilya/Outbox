/** Task 6 replaces this stub with a tenant OAuth token-backed Slack API call. */
export async function notifyRateLimitReached(input: {
  tenantId: string;
  senderId: string;
  maxPerHour: number;
}): Promise<void> {
  void input;
}
